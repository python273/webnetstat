name: webnetstat CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: install deps
      run: apt install nim libpcap
    - name: make
      run: make build_release
